///
/// HybridAppUpdaterSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `AppUpdateStatus` to properly resolve imports.
namespace margelo::nitro::minhnc::appupdater { struct AppUpdateStatus; }
// Forward declaration of `SmartReviewState` to properly resolve imports.
namespace margelo::nitro::minhnc::appupdater { struct SmartReviewState; }

#include <string>
#include <NitroModules/Promise.hpp>
#include "AppUpdateStatus.hpp"
#include <optional>
#include <functional>
#include "SmartReviewState.hpp"

namespace margelo::nitro::minhnc::appupdater {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `AppUpdater`
   * Inherit this class to create instances of `HybridAppUpdaterSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridAppUpdater: public HybridAppUpdaterSpec {
   * public:
   *   HybridAppUpdater(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridAppUpdaterSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridAppUpdaterSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridAppUpdaterSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::string getCurrentVersion() = 0;
      virtual std::string getBundleId() = 0;
      virtual std::shared_ptr<Promise<void>> openStore(const std::string& storeId) = 0;
      virtual std::shared_ptr<Promise<void>> openStoreReviewPage(const std::string& storeId) = 0;
      virtual std::shared_ptr<Promise<AppUpdateStatus>> checkPlayStoreUpdate(std::optional<bool> debugMode) = 0;
      virtual std::shared_ptr<Promise<void>> startInAppUpdate() = 0;
      virtual std::shared_ptr<Promise<void>> startFlexibleUpdate(const std::function<void(double /* bytesDownloaded */, double /* totalBytes */)>& onProgress) = 0;
      virtual std::shared_ptr<Promise<void>> completeFlexibleUpdate() = 0;
      virtual std::shared_ptr<Promise<void>> requestInAppReview() = 0;
      virtual double getLastReviewPromptDate() = 0;
      virtual void setLastReviewPromptDate(double timestamp) = 0;
      virtual SmartReviewState getSmartReviewState() = 0;
      virtual void setSmartReviewState(const SmartReviewState& state) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "AppUpdater";
  };

} // namespace margelo::nitro::minhnc::appupdater
